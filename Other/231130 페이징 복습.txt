어차피 포이치 같이 돌리니까 마지막 페이지는 덜 돈다.

페이지버튼이 일정숫자가 되면 멈추는 법은 결국 삭제되지 않은 총 레코드수를 계산하고
그 수를 넘어서는 페이지수*리스트수 가 되면 멈추는 원리이다.

알아야 하는 건 총 레코드 수(보드타입마다 바뀐다: $store.state.totalBoard), ->beforeCreate->dispatch
화면에 보이는 페이지수(스타트를 기억해야한다: $store.state.cluc), 
리스투수($store.state.page_limit)이다.



지금 총 개수를 카운트했다.
총 개수에서 리미트를 나누어서 그 이상이 나오는 값만 어떻게 하면 되지 않을까

지금 페이지의 시작 페이지버튼 넘버 필요
지금페이지에서 페이지버튼 넘버*리미트가 총개수보다 크면 반복 종료



시작페이지 넘버 = culc - (버튼리밑 - 1)

맥스페이지 - 시작페이지 < 페이지 버튼 리미트
=> 맥스페이지 - 시작페이지 만큼 돌기














